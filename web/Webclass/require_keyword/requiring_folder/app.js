let {first,second}=require("./folder/index")

console.log(first.pahla(),second.dusara());


// when we import a folder it should have index.js file where we import all files present in that folder and export them whichever is required