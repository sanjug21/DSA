var x=100;
console.log(x)
{
    var x=10;
    const y=20;
    
}

{
    let a=100;
    const b=200;
}
console.log(x)
//shadowing -->when a variable with the same name is declared in an inner scope as a variable in an outer scope

// let and const are stored in a separate space known as script

//A temporal dead zone (TDZ) is the block where a variable is inaccessible until the moment the computer initializes it with a value.